# Deuda técnica — Finexia backend

> Registro de mejoras detectadas durante la migración de arquitectura
> ([`ARCHITECTURE_MIGRATION.md`](./ARCHITECTURE_MIGRATION.md)) que **NO** se
> hacen en los PRs de migración (regla: mover ≠ mejorar). Cada entrada indica
> dónde se detectó y qué se propone. Priorizar al cerrar la Fase 8.

| # | Detectado en | Descripción | Propuesta |
|---|---|---|---|
| 1 | Fase 0 | **Bug corregido (2026-07-23).** `platform/httpx` define `Kind` + `Tagged`/`AsNotFound`/`AsBadRequest`/`AsConflict`/`AsTooManyRequests`, y `FromDomain` resuelve el status **por tipo** (`errors.As`) con **fallback** al mapeo por substring congelado (contrato de cliente intacto). La **familia _not found_ está tipada en todos los dominios** (`auth` sentinels de sesión/2FA/verificación/invitación/password-reset + `not found account`/`not found session`; `user` `ErrUserNotFound`; `market` `ErrAssetNotFound`/`ErrExchangeRateNotFound`; `portfolio` sentinels en `errors.go`+`rates.go`; `marketing` no tiene errores de dominio propios). Con eso el bug reconocido (un `not found` envuelto por `failed`/`invalid` daba 400) queda corregido en toda la app. **Pendiente (opcional):** las categorías 400/409/429/500 siguen usando el fallback por substring; tiparlas una a una permitiría retirar el fallback, pero implicaría revisar quirks de status caso por caso (p. ej. `"too many rows"`→429, `"invitation already accepted"`→500). | Tipar el resto de categorías por dominio si se decide retirar el fallback, revisando cada status contra el contrato. |
| 2 | Fase 0 | `handlers.GetParamID` nunca devuelve error: la firma `(string, error)` es ruido y sus llamadores manejan un error imposible. | Eliminar el retorno de error (o el helper completo) al mover cada handler a su módulo. |
| 3 | Fase 0 | La ruta `GET /portfolios/id` (lista de portfolios del usuario) tiene un path atípico; lo esperable sería `GET /portfolios`. | Es un cambio de contrato HTTP: coordinar con frontend fuera de la migración (alias + deprecación). |
| 4 | Fase 0 | `repositories/` (0% cobertura) solo se prueba indirectamente; no hay tests de integración contra Postgres real. | Evaluar tests de integración con testcontainers para las implementaciones `postgres.go` de cada módulo tras la Fase 8. |
| 5 | Fase 0 | **Resuelto (2026-07-23).** Los 5 schedulers ad-hoc (0% cobertura, cada uno duplicando el patrón ticker+run) fueron reemplazados en la Fase 7 por el runner genérico `Job`/`Scheduler.Register`, que **sí** está cubierto: `internal/scheduler` está al **98.5%** (`scheduler_test.go`, `root_test.go`) — ejecución, reintentos con backoff, skip de solapamiento, cancelación por contexto, recuperación de panics y las políticas de schedule (`Every`/`DailyAt`/`WeeklyAt`/`Delayed`/`Jitter`). Los jobs de cada módulo quedaron como métodos puros del servicio, testeados en su propio paquete. | — |
| 6 | Fase 4 (PR A) | `auth.Module.RequireAuth` valida el token con un `context.Context` capturado en el arranque (`Deps.Ctx`), herencia del legacy `middlewares.New(ctx, …)`. | Usar el contexto de la request (`c.Context()`/`c.RequestCtx()`) en `TokenProcessorFunc` y eliminar `Deps.Ctx`. |
| 7 | Fase 4 (PR A) | **Resuelto (2026-07-23).** Las duplicaciones eran copias temporales que vivían en los paquetes legacy (`services/user.go`, `repositories/user.go`, `services/token_helpers.go`), borrados por completo en la Fase 8. Las funciones que quedan (`createUser`/`Register` en `auth/postgres.go`, `sendPasswordChangedAlert`/`sanitizeIP`/`truncate` en `auth` y `user`) son ya el **dueño único** de cada dominio, no copias del legacy. Verificado: no queda ningún paquete `services/`/`repositories/` del que sean copia. | — |
| 8 | Fase 4 (PR A) | **Resuelto (2026-07-23).** El módulo auth ya no recibe `*config.Env`: se extrajo `auth.Config` (`internal/auth/config.go`) con exactamente los 13 campos que el dominio consume, poblado por `app` vía el helper `authConfig(env)`. `auth` dejó de importar `platform/config` (verificado con grep) y sus tests construyen un `auth.Config` pequeño en vez del Env completo. Es el patrón a replicar en los demás módulos, que hoy siguen tomando `*config.Env`. | Replicar el patrón `Config` propio en `user`/`portfolio`/`market`/`notification` cuando toque. |
| 9 | Fase 4 (PR A) | `GetUserByID`/`GetUserByEmail` viven en `auth.AccountStore` pero pertenecen al dominio user (tablas users/roles). | En Fase 5, `auth` debe consumirlos vía una interfaz local implementada por el servicio del módulo `user`. |
| 10 | Fase 4 (PR C) | `GET /users/waitlist` (lectura admin de la waitlist) se sirve desde `auth.AdminRoutes` porque forma parte del dashboard de invitaciones, aunque el dato es del módulo marketing. | Evaluar en Fase 8 mover la ruta a marketing cuando exista un guard admin compartido entre módulos. |
| 11 | Fase 6 (revisión) | `portfolio` bundlea servicio + `postgres.go` + `handler.go` en un paquete; los tests restaurados cubren la capa de servicio (37%), pero `postgres.go` y `handler.go` no tienen tests unitarios. | Añadir tests de handler (con app de Fiber en memoria, como `internal/app/app_test.go`) e integración de `postgres.go` (testcontainers, ver #4). |
| 12 | Fase 6/7 (revisión) | **Resuelto (2026-07-22).** El catálogo de assets (tipo `Asset`, `AssetType`, persistencia, servicio e import) se movió a `market`; `portfolio` referencia `market.Asset` y lo consume vía su interfaz local `AssetReader`. La dependencia quedó `portfolio → market` (market ya no importa portfolio) y `portfolio.Repository` bajó de 32 a 27 métodos. El parser de hojas de cálculo compartido se extrajo a `platform/spreadsheet`. `portfolio` conserva solo la lectura de exchange-rates para conversión de divisa de visualización. | — |
| 13 | Fase 6 (revisión) | Archivos de producción > 500 líneas (criterio de éxito). **Troceados en la revisión de cierre**: `portfolio/import.go` 911 → `import.go` (382) + `import_mapping.go` (338) + `import_parse.go` (218); `portfolio/handler.go` 551 → `handler.go` (282) + `handler_transactions.go` (282); `auth/service.go` 669 → `service.go` (364) + `service_token.go` (322). **Queda** `auth/postgres.go` (502), justo en el umbral ~500. | Partir `auth/postgres.go` por sub-store (account/session/refresh/2FA/verification) si crece; hoy está en el límite. |
| 14 | Fase 8 (revisión) | **Resuelto (2026-07-22).** `docs/ARCHITECTURE.md` creado; cobertura total medida (39.4% vs 42.6% de línea base, layout distinto); `portfolio.Repository` agrupado en sub-interfaces cohesivas (PortfolioStore/PlatformStore/RateStore/TransactionStore/SnapshotStore) y, tras mover assets a market (#12), su union quedó en **27 métodos** (bajo el criterio ~30). | — |
