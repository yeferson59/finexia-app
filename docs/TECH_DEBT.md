# Deuda técnica — Finexia backend

> Registro de mejoras detectadas durante la migración de arquitectura
> ([`ARCHITECTURE_MIGRATION.md`](./ARCHITECTURE_MIGRATION.md)) que **NO** se
> hacen en los PRs de migración (regla: mover ≠ mejorar). Cada entrada indica
> dónde se detectó y qué se propone. Priorizar al cerrar la Fase 8.

**Estado (2026-07-25):** 14 de 15 entradas resueltas. Queda abierta la **#4**
(tests de integración de las implementaciones `postgres.go` contra un Postgres
real), que necesita Docker/testcontainers y por eso no entra en las tandas
actuales. La #3 tiene un residuo menor: retirar el alias deprecado
`GET /portfolios/id` cuando los logs no registren tráfico.

| # | Detectado en | Descripción | Propuesta |
|---|---|---|---|
| 1 | Fase 0 | **Resuelto (2026-07-23).** `platform/httpx` define `Kind` + `Tagged`/`AsBadRequest`/`AsNotFound`/`AsConflict`/`AsTooManyRequests`; `FromDomain` resuelve el status **solo por tipo** (`errors.As`) y el **mapeo por substring fue retirado** (un error sin etiquetar es 500). Todos los dominios etiquetan sus errores en el origen: not-found (`auth`/`user`/`market`/`portfolio`), bad-request (credenciales, validación de import, spreadsheet inválido, etc.), conflict (`user already exists`; violación de unique constraint de la waitlist traducida en la repository vía código `23505`→`ErrWaitlistEmailExists`) y too-many (lockout de login, `too many rows`). Los sentinels de dominio de auth (2FA/verificación/invitación/password-reset) ya se mapeaban por `errors.Is` explícito en los handlers y no dependían del substring. Los errores internos (proveedor de precios, S3, DB) quedan sin etiquetar → 500 (más correcto que el 400 accidental que daban antes por contener `invalid` en el texto del proveedor). Status quirks preservados: `too many rows`→429. | — |
| 2 | Fase 0 | **Resuelto (2026-07-24).** El helper compartido `handlers.GetParamID` desapareció con el paquete `handlers/` en la Fase 8. Cada módulo tiene hoy su propio `getParamUUID(c, name) (uuid.UUID, error)` (`auth`, `user`, `portfolio`, `market`), cuyo error **sí** es real: viene de `uuid.Parse` y los handlers lo traducen a un 400 (`"Invalid portfolio ID"`, etc.). Ya no queda ningún retorno de error imposible; cubierto por los tests de handler de cada módulo (p. ej. los casos `rejects a non-uuid …` de `portfolio/handler_crud_test.go`). | — |
| 3 | Fase 0 | **Resuelto (2026-07-24).** `GET /portfolios` es ahora la ruta canónica de la lista de portfolios del usuario. `GET /portfolios/id` se conserva como **alias deprecado**: mismo handler y misma respuesta, más las cabeceras de RFC 8594 `Deprecation: true` y `Link: </portfolios>; rel="successor-version"`, que permiten detectar en los logs quién sigue llamando al path viejo antes de retirarlo. No hizo falta coordinar con frontend: `frontend/src` nunca llamó a `/portfolios/id` (solo la documentación lo listaba). `docs/API.md` §2.7 actualizado; ambos paths cubiertos por `TestHandlerGetPortfolios`. | Retirar el alias cuando los logs no registren tráfico. |
| 4 | Fase 0 | `repositories/` (0% cobertura) solo se prueba indirectamente; no hay tests de integración contra Postgres real. **Sigue abierto**: es el único hueco que queda tras cubrir los handlers (#11) y requiere Docker (testcontainers), por lo que no entra en esta tanda. | Evaluar tests de integración con testcontainers para las implementaciones `postgres.go` de cada módulo. |
| 5 | Fase 0 | **Resuelto (2026-07-23).** Los 5 schedulers ad-hoc (0% cobertura, cada uno duplicando el patrón ticker+run) fueron reemplazados en la Fase 7 por el runner genérico `Job`/`Scheduler.Register`, que **sí** está cubierto: `internal/scheduler` está al **98.5%** (`scheduler_test.go`, `root_test.go`) — ejecución, reintentos con backoff, skip de solapamiento, cancelación por contexto, recuperación de panics y las políticas de schedule (`Every`/`DailyAt`/`WeeklyAt`/`Delayed`/`Jitter`). Los jobs de cada módulo quedaron como métodos puros del servicio, testeados en su propio paquete. | — |
| 6 | Fase 4 (PR A) | **Resuelto (2026-07-24).** La validación de sesión se movió del `TokenProcessorFunc` de jwtware (que no recibe el `fiber.Ctx`) al `SuccessHandler`, donde `RequireAuth` llama a `Service.ValidateToken(c, …)` con el **contexto de la request** en lugar del capturado al arranque. `Deps.Ctx` y `Module.ctx` fueron eliminados (junto con el parámetro `ctx` de `app.buildModules`). jwtware sigue verificando firma y parseando claims; el orden de fallo (firma → sesión) sigue devolviendo 401 igual que antes. Se añadió `TestRequireAuth` (happy-path + rechazo de sesión), que antes no existía. | — |
| 7 | Fase 4 (PR A) | **Resuelto (2026-07-23).** Las duplicaciones eran copias temporales que vivían en los paquetes legacy (`services/user.go`, `repositories/user.go`, `services/token_helpers.go`), borrados por completo en la Fase 8. Las funciones que quedan (`createUser`/`Register` en `auth/postgres.go`, `sendPasswordChangedAlert`/`sanitizeIP`/`truncate` en `auth`) son ya el **dueño único** de cada dominio, no copias del legacy. La última copia real —`sendPasswordChangedAlert` duplicado en `user`— desapareció al mover el cambio de password a `auth` (ver #15). Verificado: no queda ningún paquete `services/`/`repositories/` del que sean copia. | — |
| 8 | Fase 4 (PR A) | **Resuelto (2026-07-23).** El módulo auth ya no recibe `*config.Env`: se extrajo `auth.Config` (`internal/auth/config.go`) con exactamente los 13 campos que el dominio consume, poblado por `app` vía el helper `authConfig(env)`. `auth` dejó de importar `platform/config` (verificado con grep) y sus tests construyen un `auth.Config` pequeño en vez del Env completo. Es el patrón a replicar en los demás módulos, que hoy siguen tomando `*config.Env`. **Réplica completada (2026-07-24):** `user.Config` (`PublicURL`, `FrontendURL`), `portfolio.Config` (`FrontendURL`) y `notification.Config` (`PublicURL`); `market` recibía un `*config.Env` que no leía y el campo se eliminó del todo. `app` los puebla con `userConfig`/`portfolioConfig`/`notificationConfig`, junto al `authConfig` ya existente. Ningún módulo de dominio importa ya `platform/config` — lo fija `TestModulesOwnTheirConfig` en `internal/app/arch_test.go`. | — |
| 9 | Fase 4 (PR A) | **Resuelto (2026-07-24).** Las dos consultas eran una copia literal de `user.Repository.GetByID`/`GetByEmail` (mismas columnas, mismo JOIN con `roles`, mismo helper de scan). Ahora viven solo en el dominio user: `auth` declara la interfaz local `auth.UserReader` y la consume por el slot `Stores.Users`; `AccountStore` se queda con la parte de credenciales (`accounts`, `Register`). `auth/postgres.go` perdió ambas consultas y los helpers `userCols`/`scanUserWithRole` que ya no usaba (502 → 455 líneas). `user.Service` ganó `GetUserByEmail` junto al `GetUserByID` que ya tenía. El lector se pasa por constructor (`auth.Deps.Users`): `app` construye `user.NewService(...)` antes que `auth`. Eso fue posible tras devolver a `auth` las invitaciones y el cambio de password (ver #15), que eran lo único que hacía a `user` importar `auth`. | — |
| 10 | Fase 4 (PR C) | **Resuelto (2026-07-24).** Con el guard admin ya compartido (`authMiddleware.RequireAuth`/`RequireAdmin`, que consumen `user`/`market`/`portfolio`), la ruta y su handler se movieron al módulo `marketing`, dueño del dato. **El contrato HTTP no cambia**: mismo path `GET /users/waitlist`, mismo envelope y mismos guards. Es una ruta terminal fuera del grupo `/users`, así que aplica ella misma `RequireAuth` + limitador + `RequireAdmin`; `app` monta `marketing` antes que `user`, que es lo que evita que `GET /users/:id` la capture (ya lo comprobaba `TestAppWiresAndRoutes`). `marketing` recibe los guards por constructor (`marketing.Deps.AuthMiddl`), en el segundo paso de su construcción: su servicio se crea antes que `auth` (que lo consume) y el módulo después. **Sin guard la ruta no se registra** — un error de cableado da 404, nunca un listado abierto. Efecto colateral: desaparece la dependencia `user → marketing`. | — |
| 11 | Fase 6 (revisión) | **Handlers resueltos (2026-07-24).** Los 25 handlers HTTP del módulo tienen ya test contra una app de Fiber en memoria: `handler_crud_test.go` (CRUD de portfolios y plataformas, entries, transacciones, allocation, growth y las rutas de assets servidas por `market` — incluido que el precio manual es admin-only y que un 403 no llega al servicio), `handler_export_test.go` (los tres export xlsx, verificados sobre las cabeceras de descarga **y** sobre las filas del workbook parseado) y `handler_import_test.go` (el par multipart preview/commit: campo `file`, `mapping`/`defaults` malformados, mapping obligatorio e ids no-UUID). Cobertura de `internal/portfolio`: **39.1% → 64.9%**. | Falta `postgres.go`, que necesita una base de datos real: ver #4 (testcontainers). |
| 12 | Fase 6/7 (revisión) | **Resuelto (2026-07-22).** El catálogo de assets (tipo `Asset`, `AssetType`, persistencia, servicio e import) se movió a `market`; `portfolio` referencia `market.Asset` y lo consume vía su interfaz local `AssetReader`. La dependencia quedó `portfolio → market` (market ya no importa portfolio) y `portfolio.Repository` bajó de 32 a 27 métodos. El parser de hojas de cálculo compartido se extrajo a `platform/spreadsheet`. `portfolio` conserva solo la lectura de exchange-rates para conversión de divisa de visualización. | — |
| 13 | Fase 6 (revisión) | Archivos de producción > 500 líneas (criterio de éxito). **Troceados en la revisión de cierre**: `portfolio/import.go` 911 → `import.go` (382) + `import_mapping.go` (338) + `import_parse.go` (218); `portfolio/handler.go` 551 → `handler.go` (282) + `handler_transactions.go` (282); `auth/service.go` 669 → `service.go` (364) + `service_token.go` (322). **Queda** `auth/postgres.go` (502), justo en el umbral ~500. **Resuelto (2026-07-24).** `auth/postgres.go` quedó en 455 líneas al mover las consultas de users/roles al módulo user (#9) y luego se partió por sub-store, completando el patrón que 2FA/verification/password-reset/invitations ya seguían: `postgres.go` (124, tipo compartido + `AccountStore`), `postgres_session.go` (217, `SessionStore` + known-login-IPs) y `postgres_refresh_token.go` (147, `RefreshTokenStore`). Puro movimiento de código: ninguna query, firma o comportamiento cambió. Ningún fichero de producción supera hoy las 500 líneas (el mayor es `postgres_session.go`, 217). | — |
| 14 | Fase 8 (revisión) | **Resuelto (2026-07-22).** `docs/ARCHITECTURE.md` creado; cobertura total medida (39.4% vs 42.6% de línea base, layout distinto); `portfolio.Repository` agrupado en sub-interfaces cohesivas (PortfolioStore/PlatformStore/RateStore/TransactionStore/SnapshotStore) y, tras mover assets a market (#12), su union quedó en **27 métodos** (bajo el criterio ~30). | — |
| 15 | Cierre (2026-07-25) | **Resuelto (2026-07-25).** Al cerrar #9 y #10 el cableado quedó con dos setters (`SetUsers`, `SetAdminGuard`) porque el grafo de módulos era cíclico. La causa no era de cableado sino de propiedad: `user` guardaba cuatro handlers de `/users/invitations*` que eran *pass-through* puros a `auth.Service` (y exponían `auth.Invitation` en su DTO), y su `UpdatePassword` escribía `accounts.password` —tabla de `auth`— duplicando lo que `ConsumePasswordReset` ya hacía. Ambos volvieron a `auth`, junto con `ChangePassword` y su alerta de seguridad (de la que `auth` ya tenía una copia idéntica). Con eso `user` dejó de importar `auth` y el grafo pasó a ser acíclico. `user` y `marketing` se construyen ahora en dos pasos (`NewService` → `New`), de modo que sus servicios existen antes que `auth` y sus rutas después de los guards: **no queda ningún setter, todo es inyección por constructor**. Los paths HTTP no se movieron. Lo fija `TestServiceFirstModulesStayIndependent` y, para el orden de registro bajo `/users`, `TestAppWiresAndRoutes`. | — |
