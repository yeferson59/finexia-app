/**
 * Feature `legal` — superficie pública.
 *
 * `CookieNotice` lo consume el layout raíz (`routes/+layout.svelte`) y está
 * disponible para las páginas legales.
 */
export { default as CookieNotice } from './cookie-notice.svelte';
