// El blog es contenido estático: los artículos son archivos del repositorio y
// solo cambian cuando se despliega. Se prerenderiza entero —índice, artículos,
// etiquetas y el feed— para servirlo como HTML sin pasar por el servidor.
export const prerender = true;
